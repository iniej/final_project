from imdbpie import Imdb
from django.contrib.auth.decorators import login_required

# def get_movies(title):
#     url = 'http://api.example.com/books'
#     params = {'title': title}
#     r = requests.get(url, params=params)
#     movies = r.json()
#     movies_list = {'movies':movies['results']}
#     return movies_list
